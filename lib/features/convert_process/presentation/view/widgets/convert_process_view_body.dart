import 'package:flutter/material.dart';

class ConvertProcessViewBody extends StatelessWidget {
  const ConvertProcessViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text('Jpg to PDF'),
      ],
    );
  }
}